# Environment files (secrets)
.env
.env.*
.env.local
.env.production
.env.*.local
*.env
!.env.example

# Python
__pycache__/
*.pyc
.venv/

# Node
node_modules/
dist/

# OS
.DS_Store

# Logs
*.log

# Windows artifacts
nul
~$*

# Large binary databases (use Git LFS if needed later)
*.db
*.sqlite
*.sqlite3

# Reorganized data hierarchy
# Archive contains old versions and duplicates - no need to track
/data/archive/
# State files are runtime-generated
/data/state/
# Qdrant vector stores (binary, regenerable)
/data/qdrant/
/qdrant/
Engine8_Knowledge/data/qdrant/
# Legacy stale copies (should be empty after migration)
/data/bullhorn_master.db

# Qdrant lock files
*.lock
repomix-*.xml

# Antigravity skills (pulled from external repo, reinstall via script)
.claude/skills/*/
!.claude/skills/PTS_*.md
!.claude/skills/pts-bd-pipeline/
!.claude/skills/pts-federal-intel/
!.claude/skills/pts-contact-ops/
!.claude/skills/pts-arch-migration/

# Auto Claude generated files
.auto-claude/
.auto-claude-security.json
.auto-claude-status
.claude_settings.json
.worktrees/
.security-key
logs/security/
